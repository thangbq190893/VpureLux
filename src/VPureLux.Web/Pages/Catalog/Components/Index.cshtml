@page
@model VPureLux.Web.Pages.Catalog.Components.IndexModel
@inject Microsoft.Extensions.Localization.IStringLocalizer<VPureLux.Localization.VPureLuxResource> L
@{
    ViewData["Title"] = L["Catalog:Components"];
    var canEdit = Model.CanEdit.ToString().ToLowerInvariant();
    var canViewPricingContext = Model.CanViewPricingContext.ToString().ToLowerInvariant();
}

@section scripts {
    <abp-script src="/Pages/Catalog/CatalogIndex.js" />
    <abp-script src="/Pages/Catalog/Components/Index.js" />
}

<div data-catalog-index
     data-create-view-url="Catalog/Components/CreateModal"
     data-edit-view-url="Catalog/Components/EditModal"
     data-details-view-url="Catalog/Components/DetailsModal"
     data-table-selector="#ComponentsTable"
     data-can-edit="@canEdit"
     data-can-view-pricing-context="@canViewPricingContext"
     data-saved-message="@L["Catalog:ComponentSavedSuccessfully"]"
     data-activated-message="@L["Catalog:ComponentActivatedSuccessfully"]"
     data-deactivated-message="@L["Catalog:ComponentDeactivatedSuccessfully"]"
     data-status-success="@(string.IsNullOrWhiteSpace(Model.StatusMessageKey) ? string.Empty : L[Model.StatusMessageKey].Value)"></div>

<div class="vpl-page">
    <div class="vpl-page-header">
        <h2 class="vpl-page-title mb-0">@L["Catalog:Components"]</h2>
        <div class="vpl-btn-toolbar">
            @if (Model.CanCreate)
            {
                <a asp-page="/Catalog/Components/Create" class="btn btn-primary" data-catalog-create><i class="fa fa-plus"></i> @L["Catalog:NewComponent"]</a>
            }
        </div>
    </div>

    <abp-card class="vpl-card-dense">
        <abp-card-body>
            <form id="ComponentsSearchForm" class="mb-3">
                <abp-row>
                    <abp-column size-md="_4">
                        <input asp-for="Keyword" id="ComponentsKeyword" class="form-control" placeholder="@L["Search"]" />
                    </abp-column>
                    <abp-column size-md="_4">
                        <abp-button button-type="Secondary" type="submit">@L["Search"]</abp-button>
                        <abp-button button-type="Light" type="button" id="ComponentsClearButton">@L["Clear"]</abp-button>
                    </abp-column>
                </abp-row>
            </form>
            <abp-table id="ComponentsTable" striped-rows="true" class="vpl-table-dense">
                <thead>
                <tr>
                    <th>@L["Catalog:Image"]</th>
                    <th>@L["Catalog:Code"]</th>
                    <th>@L["Catalog:Name"]</th>
                    <th>@L["Catalog:Unit"]</th>
                    <th>@L["Catalog:Status"]</th>
                    @if (Model.CanViewPricingContext)
                    {
                        <th class="text-end">@L["Catalog:CurrentComponentSuggestedPrice"]</th>
                    }
                    <th class="text-end">@L["Actions"]</th>
                </tr>
                </thead>
            </abp-table>
        </abp-card-body>
    </abp-card>
</div>
