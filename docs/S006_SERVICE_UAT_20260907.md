# S-006 Service UAT And Production Readiness

Status: IN_PROGRESS, final publish gate pending. Production rollout is NOT authorized. One acceptance-case discrepancy remains explicitly conditional below.
Started 2026-09-07, branch codex/warranty-release-review, baseline `0a7eb72`.

## Authorization And Targets

- User explicitly authorized read/write VPL, its verified backup, isolated rehearsal/empty databases, reviewed pending migrations and prefixed UAT fixtures; real SQL Server/Redis and local test runtime allowed.
- Production catalog `VPureLux` is forbidden, including reads, migration history, backup, runtime, migration, smoke, restart, config/symlink and deploy. No push.
- Configured SQL endpoint: `180.93.99.150`. `@@SERVERNAME` returns `VPureLux` (instance name), but every connection uses a separately guarded catalog: `VPL`, `VPL_SERVICE_REHEARSAL_20260907_113345` or `VPL_SERVICE_EMPTY_20260907_113345`. Instance name is not database identity.
- Source configuration InitialCatalog must equal VPL before creating a connection. Clones require an exact S006 allowlist pattern. Each mutation phase reproves DB_NAME. No production connection was opened, no server-wide catalog inventory was run, and no production environment file was read.

## Completed Migration Gates

| Gate | Result / evidence |
| --- | --- |
| Preflight | Expected branch/HEAD, tracked clean; only two user-owned documents untracked |
| Initial VPL inventory | SELECT only, 20 migrations ending at Sales V1; original AddServiceModule exists |
| Historical Service schema | PASS: 4 tables, 86 columns, 18 indexes; PK/FK/default/type/nullability verified against original migration operations |
| Legacy fingerprint baseline | 41 App tables, per-primary-key SHA256 of original full rows and factual rows; original column lists retained |
| Backup | `/var/opt/mssql/data/VPL-pre-service-s006-20260907_113345.bak`, COPY_ONLY/CHECKSUM, 23,215,104 bytes, server timestamps 2026-09-07 11:33:46..11:33:47 |
| VERIFYONLY | PASS with CHECKSUM; backup header has one VPL backup set and CopyOnly=true |
| Rehearsal clone | Restored to `VPL_SERVICE_REHEARSAL_20260907_113345`; original Service migration not rerun |
| Clone migration | All four pending Service migrations applied; 20 -> 24 history entries |
| Clone legacy reconciliation | PASS 41/41; no changed/missing original full rows or factual rows; no original column changes |
| Empty SQL Server chain | PASS: all 24 migrations via EF on `VPL_SERVICE_EMPTY_20260907_113345`; historical Service creates once |
| Current schema comparison | Empty and upgraded clone Columns/Indexes/ForeignKeys/Checks identical across App tables |
| EF model drift | NONE, offline design-time check |
| VPL pre-migration recheck | PASS 41/41 against initial baseline |
| VPL migration | Four reviewed migrations applied through EF only, no DbMigrator seed; history 20 -> 24 |
| VPL post-migration reconciliation | PASS 41/41; no original full-row/factual hash or original column change |

Pending chain applied, exactly once each:

1. `20260906174229_AddServiceWorkCatalogFields`
2. `20260906185742_AddServiceOrderWorkflow`
3. `20260906200825_AddServiceCompletionFacts`
4. `20260907021302_AddServicePaymentSettlement`

Reviewed generated SQL: `artifacts/s006/pending-service.sql`, SHA256 `B843100C3324670F0CED53544C943266285004A5B9E26819D509DBCB4CE5D8BD`.
No business DML, backfill, Sales stored-procedure changes, historical table recreation or manual migration-history insert. EF-generated history inserts are part of the migrations, not history repair.

### Rehearsal Execution Finding

The first local script runner sent each GO-delimited migration as one batch. The fourth batch failed SQL binding of a filtered index to a newly added column (`Invalid column name 'VoidIdempotencyKey'`). First three migrations had committed; the fourth did not add columns/history or alter business data. Original 41 table fingerprints still matched at that checkpoint.

Executing the existing migration through EF's normal separate-command executor succeeded without changing migration source or history. The empty-chain run and actual VPL upgrade both used EF and passed. The evidence runner was changed to `Invoke-S006EfMigration.ps1`; do not reuse a raw GO batch runner for this script. No manual repair or business DML was used.

### Real SQL Report Defect Found And Fixed

Actual execution on the clone failed with SQL 8117: `Operand data type NULL is invalid for count_big operator`. Sales-only report constant-folded `LongCount(CostIncomplete == true)` into invalid `COUNT_BIG(NULL)`. SQLite and mere SQL translation had not proven server execution.

Added a reproducing SQL-translation regression (Sales fails, other two source cases pass), then replaced the conditional count with a bigint conditional sum. This changes no business semantics, source data, Sales APIs or migration. Regression is now 3/3 and real SQL Server list/count/summary passes for Sales, Service and All. Original failure JSON/SQL/TRX are retained.

Real legacy report reconciliation after the fix: Sales revenue 22,800,000 + Service revenue 450,000 = consolidated 23,250,000. The historical refunded Sales order shows GrossPosted 8,000,000, GrossRefunded 2,000,000, NetPaid 6,000,000. Its receipt/refund records were only read. Existing Sales API naming/adjustment defects remain untouched.

## Legacy Material And Dates

- Exactly one historical completed Material line exists: order `e27239d9-37e3-f148-4773-3a234a99d2b7` / `SVC-202608250001`, line `49ca478a-cec2-977e-dd4d-3a234a99d2bc`.
- Persisted quantity 1 and material CostAmountSnapshot 100,000 match linked inventory line `834aaaf6-2f4f-0f70-8647-3a234aa02f81`: one allocation, quantity 1, cost 100,000. Factual fallback assessed against all available legacy completed material lines: 1/1 exact match, no mismatch/missing evidence.
- Legacy completion wall time `2026-08-25 17:24:24` remains unchanged. Live Details initially shifted it to the next day; fix `c2663ac` now renders `25/08/2026 17:24` without changing the raw API timestamp or stored facts.
- NEW records: six actual VPL Service completions at Vietnam 00:00, 00:30, 01:30, 06:59, 07:00 and 23:59 on September 6. SQL stores UTC; native HTTP Details, same-day report and previous-day exclusion, and reminder baseline September 6 all pass.
- LEGACY boundaries: six NEW synthetic legacy-shaped orders were inserted in a transaction on the isolated rehearsal clone, read through the actual EF report builder and then rolled back. Stored/report wall times match all six clocks; preceding/following date ranges exclude them. No existing timestamp was edited and no synthetic row remains. Real legacy HTML plus 12 SQLite new/legacy Razor renders complement this SQL evidence. See `legacy-sql-boundaries.json`, `Run-TimePolicy.ps1` and `SchemaAudit/LegacyBoundaries.cs`.
- Planned10/Actual12 is a money-projection/legacy case under accepted S004, not an executable expansion of S003: actual quantity is capped at planned and prices remain snapshots. Do not modify that rule just to manufacture a passing operator scenario.

## Implementation Fixes

| Commit | Reproduced defect and bounded correction |
| --- | --- |
| `5e7727a` | SQL Server Sales-only summary emitted COUNT_BIG(NULL); conditional bigint SUM, translation and real SQL execution now pass |
| `2583dbe` | Native authenticated Chrome JSON Service mutation accepted a missing antiforgery header and created one prefixed Work. Apply standard ASP.NET AutoValidateAntiforgeryToken only to ServiceWork, ServiceOrder and ServicePayment conventional controllers; missing token now 400, valid token accepted |
| `c2663ac` | Legacy Details added seven hours. Explicit `IsLegacyCompletion` DTO flag from persisted completion hash boundary; Razor shifts only new UTC facts, not old wall times |

All three are fixes to already accepted behavior; no migration or new business rule. Sales workflow/payment APIs were not changed. The antiforgery regression asserts Sales controller filters are untouched. Installed ABP 10.4.1 reported its global filter was not the most effective IAntiforgeryPolicy despite AutoValidate enabled; the deeper framework cause is not established. Do not claim a framework-wide diagnosis. Future non-cookie clients must supply the enforced Service antiforgery contract; bearer-client compatibility was not part of this operator UAT.

The rejected-token probe originally created a new Work; its ID is retained in the manifest instead of deleting evidence. An intermediate probe was inconclusive because Service was disabled (403); final Chrome evidence is `browser-antiforgery-after.json` (400).

## Runtime And Fixture Ownership

- Actual WebModule, native cookie authentication, local HTTP `localhost:5196`, real SQL VPL and real local Redis `127.0.0.1:6379`, database 13, isolated cache prefix. No in-memory distributed lock substitution.
- Runtime-only controls for feature/permission/failure injection are in the evidence project, not application assemblies. Jobs/workers/static-definition saving were disabled. CustomerCare intake disabled. No automatic migration/seed at startup.
- Created only `UATSVC_20260907_OPERATOR` identity/role for permissions. No pre-existing admin/customer/group/role mutation. Local credentials/cookies remain ignored in `uat-auth.json` / `browser-auth.json`; do not print, copy into docs or commit them.
- Fixture customer `1f11bb6a-289b-71df-d930-3a238c687fea`, warehouse `204d3e73-9ffe-f8d8-9982-3a238c688477`, main external asset `6f64050c-9cae-6cec-5e48-3a238c68a714`.
- Main Core1..3 order `79955c95-c05e-1218-0fbc-3a238c6a5be4`; advance order `d59acda4-00a2-5fc8-9e9a-3a238c690ca9`; browser money order `f34c3e70-afd7-1378-6fcc-3a238c8c887f`; second-line shortage order `83d0cd74-4d82-16a7-e27b-3a238ca77df3`.
- Full exact fixture IDs and resumable phase flags are in ignored `artifacts/s006/fixtures.json`; sanitized IDs, test evidence hashes and original-table fingerprints are source controlled in `docs/evidence/s006/verification-evidence.json`.
- Runtime stopped after final live UAT. Fixtures, backup and both isolated databases retained deliberately. No cleanup delete or restore-over-VPL.

## Operator And Data UAT

| Gate | Observed result |
| --- | --- |
| Feature off/on | Off blocks permitted Service mutation with SERVICE_009 and hides menu. Direct Warranty transitional completion succeeds on a new fixture while off. When enabled, direct replacement completion is blocked; Skip remains usable |
| Work | Create/edit, null versus zero standard cost, unit/price, inactive rejection for new selections, safe encoded HTML, native ABP modal and page 2 pass |
| Order | Create/Edit/Confirm/Start, material/labor, one asset, retained line IDs and header-only snapshots, stale edit SERVICE_019, reasoned Cancel, direct Confirmed->Complete rejection pass |
| Before Complete | No stock issue, replacement event or recognized revenue from Confirm/Start |
| External Core1..9 | Perform only 1..3 plus unknown-cost labor; only 1..3 stock/events/cycles change. Core4..9 before/after DTOs are identical at this checkpoint |
| MissingBaseline | Actual Core7 replacement creates its first factual baseline; no fabricated previous replacement |
| Policy | Current enabled -> successor; changed policy preserves old cycle and copies current 6 months/21 warning days into new cycle; disabled/missing -> completion without successor; re-enable does not backfill |
| Inactive/unmapped | Existing pending cycles must first be explicitly skipped before allowed mapping edits. Service then neither reactivates/remaps nor creates successors for these positions |
| FIFO | ServiceIssue enum 6. Core1..3 each consumes first lot 1@10k then next lot 1@20k, total30k per line; single-lot and repeated Component/depleted-first-lot cost40k pass; labor has no inventory |
| Shortage | Single-line and required allocatable-line1/short-line2 both reject; order remains InProgress. Eight before/after groups exactly equal |
| Injected failure | Test-only exception after actual stock AND care SaveChanges; transaction rolls back order, lines, transaction, lots, balances, positions, events and reminders exactly |
| Snapshot immutability | After Complete, edit current Work name/unit/price/cost, Component metadata and policy: prior order/line snapshots, revenue/cost, events, reminders and report remain identical |

## Real SQL / Redis Races

All requests used real HTTP against the SQL/Redis runtime, with simultaneous client dispatch; this is not a multi-node load benchmark. Deterministic unit barriers are additional evidence, not a replacement for live runs.

| Race | Result |
| --- | --- |
| Complete / exact Complete replay | Both200, same factual completion; no duplicated issue/event/cycle |
| Complete / Cancel | Cancel won200, Complete rejected403 SERVICE_019; one terminal state |
| Two orders / final stock | One200, other predictable rejection; final quantity/value zero, no negative balance |
| Two orders / same position | No500; serialized care updates, one pending successor |
| Payment / Complete | Payment first accepted4m, Complete actual3m; customer credit derives from existing payment, no lost cash |
| Payment / Cancel | Cancel won; Payment rejected403 SERVICE_029 after reload |
| Payment / Payment | Two4m against5m cap -> one4m posted, second rejected; never8m |
| Refund / Refund | Two4m against5m due ->4m refunded,1m remains |
| Service / direct Warranty Complete | Only Service succeeds while enabled; no500 or bypass |

## Cash And Reports

- Advance8m against planned10m: revenue0. Complete actual6m retains original Posted8m, due2m. Two factual1m refunds settle due0, revenue still6m. Exact payment/refund replay returns original records; changed payload conflicts; overpayment/over-refund reject.
- Cancel with Posted5m: Cancelled, revenue0, due5m; no automatic Void/Refund. Explicit refund settles it. Wrong-receipt Void preserves ID/reason/actor/time; original Add replay returns Voided without repost.
- Native browser modal accepted `1.500.000`, then reasoned Void, second1.5m receipt, actual1m completion and factual500k refund. Final GrossPosted1.5m - GrossRefunded.5m = NetPaid1m, recognized revenue1m. First browser attempt and successful resumed stages are separate evidence; final read-only rerun has an empty `posts` list and must not be represented as fresh writes.
- Final yearly Service: 20 completed documents, revenue15,900,000; Sales7 documents22,800,000; All27 documents38,700,000. SQL persisted Completed totals agree; non-completed states excluded. Unknown labor/material history keeps profit null, not guessed zero.
- Legacy refunded Sales report is factually8m posted -2m refunded =6m net. Existing Sales API naming discrepancy remains separate and untouched. Accepted revised/superseded/cancelled Sales report semantics additionally pass focused EF tests; no Sales workflow mutation was performed to manufacture VPL fixtures.
- Customer summary is a database aggregate, inconsistentOrderCount0. Payment/refund page2 and Works/Orders/asset/material/work lookups/Service/Consolidated page2 return distinct rows with count/skip/take.

## Security And Browser

- Actual native HTTP deny cases cover Service View/Create/Edit/Confirm/Cancel/Complete/ManagePayments/ManageWorks and Service/Consolidated/required Sales report view. Grants were restored only on the new fixture role. Restricted cost/profit fields are null in API responses.
- Eight missing-token mutation routes reject after the scoped fix, with valid-token operator writes succeeding. No browser native prompt/alert/confirm; ABP modals and encoded text tested.
- Chrome1440x960 and390x844: Works/list/Create/Edit/Details/Complete/Payment/Void/Refund/ServiceReport/Consolidated inspected, local icons visible, no JS errors or document overflow. Wide DataTables scroll inside their container. Long synthetic operator username truncates in existing theme chrome.
- Final screenshot capture waits for DataTables initialization, KPI response and finite CSS animations; earlier loading/half-faded captures were superseded. Google/external assets were blocked in browser harness; local fonts/icons and fallback text inspected, not external availability.

## SQL Plans And IO

Actual SELECT execution with STATISTICS XML/IO/TIME on VPL, five `.sqlplan` plus generated `.sql` and IO logs under `artifacts/s006/query-plans`:

| Query | Returned rows | Logical reads | SQL CPU / elapsed ms | Client wall ms |
| --- | ---: | ---: | --- | ---: |
| Service page2 | 10 | 126 | 12 / 11 | 184 |
| Service report | 10 | 206 | 13 / 11 | 352 |
| Consolidated report | 10 | 546 | 15 / 13 | 562 |
| Care pending/successor set | 2 | 6 | 9 / 5 | 87 |
| Customer money aggregate | 1 | 402 | 12 / 10 | 218 |

Small tables produce clustered scans/nested-loop lookups; no client-side N+1, explosive result multiplication, physical reads or spill found in these probes. Plans warn `ColumnsWithNoStatistics`; recommendation is an operational statistics/cardinality assessment with realistic volume before speculative indexing. No UPDATE STATISTICS, index change or new migration was executed. This is not proof of production-scale capacity. Standalone probes invoke real report/money query builders with ABP filters disabled; list/care predicates mirror source. Native HTTP security checks are separate, so do not treat these plan probes as authorization tests.

## Final Reconciliation And Regression

- Post-UAT VPL and post-synthetic-rollback clone: PASS41/41 original App tables, zero changed/missing old full/factual rows and zero original-column changes. New fixture rows are deliberately excluded from the legacy equality test. Hashes/counts are in the committed evidence index.
- Fixture material balances equal summed available lots, all nonnegative. No orphan Service lines/payments/refunds/lot allocations/reminders. Core factual FIFO quantities/costs reconcile to persisted Service snapshots; customer money consistent; completed report totals reconcile to SQL.
- Release final build:0 errors,2 known warnings (Scriban advisory, Web test entrypoint). Earlier clean builds also showed two existing OpenIddict nullable warnings. No dependency upgrade was attempted.
- Domain74, Application14, EF236 focused/broad tests passed. Final EF rerun is being recorded separately. Web: Works6, money8, pure business-report1, Warranty13 plus independently split Service/Sales methods42 all passed (70 tests, not a full Web suite). Actual NUglify regression includes the `be4a5ad` summary closure fix.
- Original grouped Web Service run12pass/8OOM and Sales20pass/1OOM are retained, not relabeled green. Their methods pass when isolated with1.5GiB heap cap and90s hang bound. A zero-test `ServiceCompletionWebTests` filter was an incorrect harness class name, not a pass; completion cases ran under the actual partial `ServiceOrderWebTests` class in split evidence.
- Offline EF `has-pending-model-changes`: NONE. PowerShell syntax and `node --check` pass. No combined full Web claim.

## Conditional Acceptance Item

**C01: planned10m / actual12m operator scenario contradicts accepted S003 quantity cap and price snapshots.** Domain monetary projection already verifies actual12/paid5 -> receivable7. The current UI/API cannot legitimately complete above its planned charge; no code or stored fact was altered to bypass this. Before claiming every requested S006 case passed, user must accept classifying this as projection/legacy-only coverage, or approve a separate estimate/change-order business design. Recommendation: keep S003 unchanged and accept the coverage exception; do not expand Service scope during release sealing.

Technical publish/static/hash checks are still pending at this intermediate evidence commit. No Severity1/2 Service defect remains observed in executed gates, but neither partial technical completion nor this recommendation authorizes production rollout.

## Evidence And Reproduction

Source-controlled harnesses: `docs/evidence/s006/`. Raw evidence is local/ignored in `artifacts/s006/` and includes:

- `vpl-before.json`, `vpl-pre-migration.json`, `vpl-after-migration.json`.
- `clone-before.json`, failed-batch checkpoint `clone-after.json`, final `clone-after-ef.json`, `empty-after.json`.
- `historical-schema-audit-r2.json` (the first comparison needed normalization of SQL Server's `CONVERT(bit,0)` default, not a schema repair).
- `backup-20260907_113345.json`, database creation evidence.
- `clone-reconciliation.json`, `vpl-pre-migration-reconciliation.json`, `vpl-migration-reconciliation.json`.
- `legacy-cost-readonly.json`, `clone-report-before-fix.json`, `clone-report-after-fix.json`, generated SQL, `sql-count-null-before.trx`, `sql-count-null-after.trx`.

Inventory/reconciliation scripts only SELECT database data and write local evidence files. They hash explicit original columns so additive nullable columns do not masquerade as changes to old facts. Evidence is not overwritten. SQL schema audit is offline by default; explicit read-smoke mode only permits named S006 rehearsal catalogs.

Never stage `docs/html.txt` or `docs/VPureLux_Sales_Flow_Design_Review_for_Codex_5_6_Sol.docx`. Keep backup and isolated databases until this gate is closed; do not restore VPL to clean fixtures and do not drop VPL.
